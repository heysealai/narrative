# Issues

This repo's issues live on the shared tracker `heysealai/seal`, never here.
Every `gh issue` call passes `-R heysealai/seal`. A PR body references an
issue as `heysealai/seal#N` (a bare `#N` resolves inside this repo). Each
issue carries the label `repo:narrative`.
